# kazokuro（家族ローゼット）作業ルール

## このアプリ
- 家族の服管理アプリ「家族ローゼット（かぞクロ）」。
- 本体は `index.html` の1ファイルだけ。バージョンは `APP_VERSION` で管理している。

## 話し方
- 相手は専門用語が分からない。報告や質問では専門用語を使わず、次の言い方に置き換える。

| これまでの言い方 | これからの言い方 |
|---|---|
| main | 本番 |
| ブランチ | 作業用のコピー |
| PR（プルリクエスト） | 本番への反映の申請 |
| マージ | 本番に反映 |
| push / コミット | 作業用のコピーに保存 |
| Routine | 定期実行 |

- 表にない専門用語も、ふだんの言葉に言い換える。
- ソースコードの変更を見せるときは「検索」「置き換え」の形で書く。

## 進め方
1. 作業用のコピーで変更し、保存する。
2. 最後に何が変わるのかを短く伝え、「本番に反映していいですか？」と聞く。
3. 「OK」をもらったら、本番への反映の申請を作り、そのまま自分で本番に反映する。相手にボタンを押してもらう必要はない。

## ロードマップ
- これから作るもの・決めることは `ROADMAP.md` にまとめてある。作業を始める前に読む。
- 作業が1つ終わったら、`ROADMAP.md` の印（☐ → ✅）と「今の状態」を更新する。
